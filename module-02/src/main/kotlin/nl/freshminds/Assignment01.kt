package nl.freshminds

/**
 * Implement the [convertToLong] function so that the tests in 'Assignment01Test.kt' pass.
 *
 * Requirements:
 * - Return 0 if [optionalNumber] is null, return the equivalent of [optionalNumber] as a long otherwise
 */
fun convertToLong(optionalNumber: Int?): Long {
    TODO()
}
